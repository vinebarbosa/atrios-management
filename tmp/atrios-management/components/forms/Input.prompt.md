Dark inset text field on `--atr-surface-1`. `focused` shows the indigo active border; `mono` switches to the monospace stack with code tracking (product codes, branch names).

```jsx
<Input placeholder="Nome do produto" />
<Input value="POR" mono focused />
```
