Tiny inline label for card meta — "novo", "auto" (with `pulse`), PR refs, counts. `tone` sets the color; `pulse` gives the breathing indigo ring used on automation badges.

```jsx
<Badge tone="primary">novo</Badge>
<Badge tone="primary" pulse icon={<GraphIcon/>}>auto</Badge>
<Badge tone="neutral" mono>#142</Badge>
```
