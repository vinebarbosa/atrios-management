Small repository tag with a colored dot — attaches a task or panel to one of the product's repos. `web`, `api`, `mobile` auto-color; pass `color` for anything else.

```jsx
<RepoChip name="api" />
<RepoChip name="web" />
```
